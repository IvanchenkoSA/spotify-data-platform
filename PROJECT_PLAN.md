# План развития проекта

Этот файл — актуальная точка продолжения работы. Перед началом новой задачи сначала сверяй статус здесь, а после завершения задачи обновляй его вместе с кодом и документацией.

## Текущая точка

Дата обновления: 2026-08-06

Готово:

- Локальная загрузка истории недавно прослушанных треков из Spotify с OAuth и watermark.
- PostgreSQL: raw-слой, `mart_listening_history` и аналитические SQL-вьюхи.
- Streamlit-дашборд: активность по дням и часам, топ треков, топ артистов и последние прослушивания.
- Обогащение артистов жанровыми тегами Last.fm.
- Жанровая аналитика: `dim_artists`, `artist_genres`, `analytics_top_genres` и график `Top genres`.
- Last.fm ingestion: постраничная загрузка скробблов в `raw_lastfm_scrobbles`, отдельный watermark и идемпотентный повторный запуск.
- Наблюдаемость: `pipeline_runs` фиксирует исход, объём и ошибки каждого запуска Spotify, Last.fm и импорта архива.
- Подготовлен импорт Spotify Extended Streaming History в `raw_spotify_extended_history`; архив ожидается от Spotify и хранится только локально.

Проверенные данные на момент обновления:

- 251 прослушивание в Spotify raw- и mart-слоях.
- 51 скроббл Last.fm в raw-слое (3–5 августа 2026 года).
- 76 артистов обработано Last.fm; у 72 есть жанровые теги.
- 348 жанровых тегов Last.fm.

## Ближайший этап — аналитика и интерфейс

Первым приоритетом стала надёжность данных: Spotify API возвращает только до 50 недавних событий. Last.fm добавлен как долгосрочный источник скробблов; raw-слои и водяные знаки источников ведутся раздельно.

Следующий конкретный шаг: запросить Extended Streaming History в Spotify и импортировать её в `raw_spotify_extended_history`. После импорта создать единый staging-слой для Spotify API, Last.fm и архива, не скрывая источник события.

- [x] Добавить фильтр периода в дашборд.
- [x] Вывести в последних прослушиваниях жанровые теги и читаемые локальные дату/время.
- [ ] Добавить фильтры по артисту и жанру.
- [ ] Добавить временную динамику по неделям и месяцам.
- [ ] Добавить аналитику по альбомам и карточки с ключевыми метриками.
- [ ] Вывести покрытие Last.fm: артисты с тегами и без тегов.

## Следующий этап — локальные рекомендации

- [ ] Спроектировать страницу «Рекомендации».
- [ ] Добавить рекомендации «послушай снова»: любимые артисты и треки, которые давно не включались.
- [ ] Добавить рекомендации альбомов уже любимых артистов, исключая знакомые релизы.
- [ ] Добавить рекомендации похожих артистов и альбомов на основе жанровых тегов Last.fm.
- [ ] Объяснять в интерфейсе, почему показана каждая рекомендация.

Spotify API не использовать как источник алгоритмических рекомендаций: его соответствующие возможности ограничены для development-приложений. Основой рекомендаций будут локальная история и жанровые теги Last.fm.

## Долгосрочный этап — локальное приложение

- [ ] Создать экран первой настройки Spotify Client ID, Client Secret и redirect URI.
- [ ] Перенести ручные команды ingestion / transform / enrichment в кнопки интерфейса.
- [ ] Сделать один локальный запуск приложения без ручного управления Docker и терминалом.
- [ ] Определить безопасное локальное хранение токенов.
- [ ] Подготовить Windows-сборку или установщик.

## Правило ведения плана и Git

При каждом завершённом изменении:

1. Обновить этот файл: отметить выполненное, добавить следующий конкретный шаг и при необходимости обновить текущие цифры.
2. Проверить изменение в объёме, соответствующем риску.
3. Создать коммит, включающий код, документацию и `PROJECT_PLAN.md`.
4. Отправить коммит в `origin/master` на GitHub.

Не хранить в GitHub `.env`, `.spotify-token.json` или выгруженные персональные данные из `data/raw/`.
